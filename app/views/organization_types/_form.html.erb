
<div id="dialog-new-organization-type" title="<%= @form_title %>">
	<p>All form fields are required.</p>

		<% form_for(@organization_type) do |f| %>
		<% if @organization_type.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@organization_type.errors.count, "error") %> prohibited this organization_type from being saved:</h2>

	      <ul>
	      <% @organization_type.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

		<div class="form_label"><%= f.label :name %></div>
		<%= f.text_field :name %>
		<br />
		
		<div class="form_label"><%= f.label :description %></div>
		<%= f.text_area :description, :rows => "5" %>
		<br />

		<div class="form_label"><%= f.label :active %></div>
		<%= f.check_box :active %>
		<br /><br />
		
		<div class="actions">
	    <%= f.submit %>
	  </div>
		
		<% end %>
</div>