<h1>Listing organization_users</h1>

<table>
  <tr>
    <th>Organization</th>
    <th>User</th>
    <th>Start date</th>
    <th>End date</th>
    <th>Head</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @organization_users.each do |organization_user| %>
  <tr>
    <td><%= organization_user.organization_id %></td>
    <td><%= organization_user.user_id %></td>
    <td><%= organization_user.start_date %></td>
    <td><%= organization_user.end_date %></td>
    <td><%= organization_user.head %></td>
    <td><%= link_to 'Show', organization_user %></td>
    <td><%= link_to 'Edit', edit_organization_user_path(organization_user) %></td>
    <td><%= link_to 'Destroy', organization_user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Organization user', new_organization_user_path %>
